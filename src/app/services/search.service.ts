import { Injectable } from '@angular/core';
import {Search} from '../model/search';
@Injectable({
  providedIn: 'root'
})
export class SearchService {
  //Search:s=new Search();

  constructor() { }
}
